//Cat extends (inherits) Animal which is beneficial because Cat is an animal so we don't have 
//rewrite all of the fields and methods in the Cat class
public class Cat extends Animal {
  public Cat() {
    this.sound = "Meow";
  }

}